capture:
  build:
    clean-command: mvn clean
    build-command: mvn install  

analyze:
  location: connect
  checkers:
    all: true
    webapp-security:

commit:
  connect:
    project: JSC
    stream: JSC_2023.3
    url: https://poc331.coverity.synopsys.com
